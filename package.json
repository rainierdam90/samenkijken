{
  "name": "watchmovietogether",
  "version": "1.0.0",
  "private": true,
  "description": "WatchMovieTogether — own signaling (PeerJS), realtime control/chat, admin monitoring and short-lived TURN credentials.",
  "main": "server/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "peer": "^1.0.2",
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  }
}
